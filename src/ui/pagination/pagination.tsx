import styled from "styled-components";

type Props = {
  pageCount: number;
  currentPage: number;
};

export const Pagination: React.FC = () => <OtherPage></OtherPage>;

const OtherPage = styled.div`
  height: 50px;
`;
